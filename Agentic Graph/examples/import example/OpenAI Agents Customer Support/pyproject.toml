[project]
name = "support"
version = "0.1.0"
description = "An OpenAI Agents SDK customer support triage system — sample for Agentic Graph"
requires-python = ">=3.10"
dependencies = [
    "openai-agents>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
